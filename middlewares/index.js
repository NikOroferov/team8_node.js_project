const ctrlWrapper = require('./ctrlWrapper');
const auth = require('./auth');
const validation = require('./validation');
const upload = require('./upload');
const equalToken = require('./equalToken');

module.exports = {
  ctrlWrapper,
  validation,
  auth,
  upload,
  equalToken,
};
